---
title: Liaison d'évènements
description: Apprennez à lier un événement à une fonction
---

import { Steps } from '@astrojs/starlight/components';
import Objective from '../../../../../components/learning-objective.astro';

<Objective class="drop-shadow-xl" objectives={[
{mainTitle: 'Objectifs de ce chapitre', title: 'Liaison d\'évènements', description: 'Apprenez à lier un événement à une fonction dans Angular.'},
]} />


## Liaison d'évènements

La liaison d'évènements permet d'écouter les évènements et d'exécuter une fonction lorsque l'évènements se produit.
Dans la situation actuelle, vous allez exécuter une fonction lorsque l'on clique sur le bouton 'Créer une tâche'.

Sur la balise HTML `<button>`, vous pouvez lier l'événement clic à une fonction en utilisant la syntaxe `(click)`.
Vous lui attribuez la fonction que vous voulez exécuter lorsque l'on clique sur le bouton.

```html ins="(click)="createTask()""
<button type="submit" class="btn btn-primary" (click)="createTask()">Créer une tâche</button>
```

Dans la classe du modèle (dans le fichier `.component.ts`/ la classe du composant), vous devez créer la fonction liée. Sans cela, Angular ne compilera pas.

### 🎓 Instructions

<Steps>

1. Modifiez le fichier `src/app/task-form/task-form.component.html`.

    ```html ins={"Ajoutez la liaison d\'évènement de soumission du formulaire": 8-9} ins="(click)="createTask()""
    // task-form.component.html
    <form>
      <label for="title" class="form-label">Titre:</label>
      <input type="text" id="title" name="title" class="form-control" [(ngModel)]="task.title">

      <label for="description" class="form-label">Description:</label>
      <textarea id="description" name="description" class="form-control" [(ngModel)]="task.description"></textarea>

      <button type="submit" class="btn btn-primary" (click)="createTask()">Créer une tâche</button>
    </form>
    ```

</Steps>

## Fonction `createTask`

Vous allez créer une fonction `createTask` pour gérer la soumission du formulaire.
Pour cette étape, vous allez simplement enregistrer la valeur du formulaire dans la console.

### 🎓 Instructions

<Steps>

1. Modifiez le fichier `src/app/task-form/task-form.component.ts`.

```typescript ins={"Ajoutez la méthode createTask": 17-20}
// task-form.component.ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-task-form',
  templateUrl: './task-form.component.html',
  styleUrls: ['./task-form.component.css']
})
export class TaskFormComponent implements OnInit {
    
  task: TaskForm = {
    title: '',
    description: ''
  };
    
  ngOnInit() {}  

  createTask(): void {
    console.log('Tâche créée', this.task);
  }
}
```

3. Ouvrez la console Devtools de votre navigateur.
4. Remplissez les champs de titre et de description et cliquez sur le bouton 'create task'.
4. En cliquant sur le bouton 'create task', vous devriez voir les données du formulaire s'afficher dans la console.

</Steps>

## ✔️ Ce que vous avez appris

Vous avez appris à écouter un événement pour déclencher une fonction.
