---
title: C'est parti !
description: Comment démarrer avec Angular
---

import { Image } from 'astro:assets';
import boilerplateImg from '../../../../assets/getting-started_boilerplate.png';
import headerImg from '../../../../assets/getting-started_header.png';
import { Steps } from '@astrojs/starlight/components';
import Objective from '../../../../components/learning-objective.astro';

<Objective class="drop-shadow-xl" objectives={[
    {mainTitle: 'Objectifs de ce chapitre', title: 'Générez une application Angular', description: 'Découvrez comment utiliser la CLI Angular pour générer une application Angular.'},
    {title: 'Exécutez l\'application', description: 'Lancez l\’application localement et consultez la page de base par défaut.'},
    {title: 'Comprendre le rechargement à chaud', description: 'Comprenez comment fonctionne le rechargement à chaud dans les applications Angular.'}
]} />

## Créez le projet

Angular fournit une interface de ligne de commande (CLI) pour générer une nouvelle application Angular, gérer le projet et exécuter l'application localement.

### 🎓 Instructions

<Steps>

1. Ouvrez un terminal et exécutez la commande suivante pour créer une application Angular :

    ```bash
    npx -p @angular/cli@17.0.0 ng new angular-introduction-course --standalone=false --style=css --ssr=false --skip-tests=true
    ```
</Steps>

:::tip
La commande inclut quelques options pour **faciliter le processus d'apprentissage**.
Apprenez-en davantage sur les options dans les <a href="/practical-course/going-further#angular-cli-options" target="_blank">Options de la CLI Angular</a>.
:::

## Ouvrez le projet

### 🎓Instructions

<Steps>
1. Ouvrez **Visual Studio Code**.
2. Cliquez sur le menu `Fichier` et sélectionnez `Ouvrir un dossier`.
3. Accédez au dossier `angular-introduction-course` et cliquez sur le bouton `Sélectionner un dossier`.
</Steps>

## Exécutez l'application

La CLI Angular fournit une commande pour lancer l'application localement.

### 🎓 Instructions

<Steps>
1. Ouvrez le terminal de Visual Studio Code.
2. Tapez la commande suivante pour démarrer l'application :

    ```bash
    ng serve
    ```

3. Ouvrez votre navigateur et accédez à `http://localhost:4200`.
4. Vous devriez voir la page de base par défaut suivante :

    <Image class="shadow-xl rounded-xl" src={boilerplateImg} alt="Un oiseau posé sur un nid d’œufs."/>
</Steps>

## Rechargement à chaud

Le rechargement à chaud est une fonctionnalité qui vous permet de voir les modifications que vous apportez à votre code sans avoir à actualiser manuellement la page.

### 🎓 Instructions

<Steps>
1. Ouvrez le fichier `src/app/app.component.html`.
2. Remplacez le contenu complet du fichier par le code suivant :

    ```html
    // app.component.html
    <header>
        <h1>Angular Practical course</h1>
    </header>
    ```

3. Revenez à votre navigateur pour voir la page suivante :

    <Image class="shadow-xl rounded-xl" src={headerImg} alt="Un oiseau posé sur un nid d’œufs."/>
</Steps>
